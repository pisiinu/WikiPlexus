import React from "react";
import ReactDOM from "react-dom/client";
import WikiPlexus from "./WikiPlexus";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WikiPlexus />
  </React.StrictMode>
);
